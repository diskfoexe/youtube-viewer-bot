services:
  - type: web
    name: youtube-viewer-bot
    env: python
    region: oregon
    plan: free
    buildCommand: |
      apt-get update && apt-get install -y wget gnupg curl unzip xvfb
      wget -q -O - https://dl.google.com/linux/linux_signing_key.pub | apt-key add -
      echo "deb [arch=amd64] http://dl.google.com/linux/chrome/deb/ stable main" >> /etc/apt/sources.list.d/google-chrome.list
      apt-get update && apt-get install -y google-chrome-stable
      pip install --no-cache-dir -r requirements.txt
    startCommand: |
      export DISPLAY=:99
      Xvfb :99 -screen 0 1024x768x24 & 
      python main.py
    envVars:
      - key: BOT_MODE
        value: LIVE
      - key: STREAM_URL
        value: https://www.youtube.com/watch?v=YOUR_STREAM_ID
      - key: VIDEO_URLS
        value: https://www.youtube.com/watch?v=VIDEO1,https://www.youtube.com/watch?v=VIDEO2
      - key: VIEWER_COUNT
        value: 3
      - key: MAX_DURATION_MINUTES
        value: 0
      - key: RESTART_INTERVAL_HOURS
        value: 4
      - key: PYTHONUNBUFFERED
        value: 1
      - key: DISPLAY
        value: ":99"
